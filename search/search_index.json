{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"pystepdowner","text":"<p>A tool to enforce the Stepdown Rule (from Uncle Bob's Clean Code) in your Python files.</p> <p>The Stepdown Rule states that code should be readable from top to bottom. Every function should be followed by those at the next level of abstraction so that we can read the program, descending one level of abstraction at a time as we read down the list of functions.</p> <p><code>pystepdowner</code> analyzes your Python files and rearranges functions and methods to adhere to this rule, prioritizing source-code call order and correctly handling complex dependency graphs.</p>"},{"location":"#installation","title":"Installation","text":"<p>You can install <code>pystepdowner</code> as a global CLI tool using <code>uv</code> (recommended):</p> <pre><code># Install from PyPI\nuv tool install pystepdowner\n\n# Or install from source locally\nuv tool install .\n</code></pre> <p>Alternatively, you can install it into any Python environment using standard <code>pip</code>:</p> <pre><code>pip install pystepdowner\n</code></pre>"},{"location":"#usage","title":"Usage","text":"<p>The CLI provides two main commands: <code>fmt</code> and <code>rw</code>.</p>"},{"location":"#1-fmt-format-and-print-to-output","title":"1. <code>fmt</code>: Format and Print to Output","text":"<p>Use the <code>fmt</code> command to format a single file and output the result to your terminal. This is useful for previewing changes without modifying the actual file.</p> <pre><code>pystepdowner fmt -i my_script.py\n</code></pre>"},{"location":"#2-rw-rewrite-files","title":"2. <code>rw</code>: Rewrite Files","text":"<p>Use the <code>rw</code> command to format and rewrite files. You can rewrite a single file or an entire directory recursively.</p>"},{"location":"#rewrite-a-single-file-in-place","title":"Rewrite a Single File In-Place","text":"<pre><code>pystepdowner rw -i my_script.py\n</code></pre>"},{"location":"#rewrite-a-single-file-and-save-to-a-new-location","title":"Rewrite a Single File and Save to a New Location","text":"<pre><code>pystepdowner rw -i my_script.py -o formatted_script.py\n</code></pre>"},{"location":"#rewrite-an-entire-directory-in-place","title":"Rewrite an Entire Directory In-Place","text":"<p>This will recursively find and format all <code>.py</code> files inside the target directory.</p> <pre><code>pystepdowner rw -i src/\n</code></pre>"},{"location":"#example","title":"Example","text":"<p><code>pystepdowner</code> transforms bottom-up code into top-down code, putting high-level logic before the helpers it calls.</p> <p>Before: <pre><code>def load_user(user_id: int) -&gt; dict:\n    return {\"id\": user_id, \"name\": \"Alice\"}\n\n\ndef format_user(user: dict) -&gt; str:\n    return f\"{user['id']}: {user['name']}\"\n\n\ndef user_controller(user_id: int) -&gt; dict:\n    user = load_user(user_id)\n    return {\"body\": format_user(user)}\n</code></pre></p> <p>After running <code>pystepdowner rw -i my_script.py</code>: <pre><code>def user_controller(user_id: int) -&gt; dict:\n    user = load_user(user_id)\n    return {\"body\": format_user(user)}\n\n\ndef load_user(user_id: int) -&gt; dict:\n    return {\"id\": user_id, \"name\": \"Alice\"}\n\n\ndef format_user(user: dict) -&gt; str:\n    return f\"{user['id']}: {user['name']}\"\n</code></pre></p>"},{"location":"cases/","title":"Cases","text":"<p>These cases progress from simple function ordering to runtime-sensitive dependencies.</p>"},{"location":"cases/#1-function-references","title":"1. Function References","text":"<p>The caller moves above the helpers it uses. Function bodies are lazy, so helpers may safely be defined below the caller.</p> <p>Before:</p> <pre><code>def d() -&gt; str:\n    return \"d\"\n\n\ndef c() -&gt; str:\n    return \"c\"\n\n\ndef b() -&gt; str:\n    return \"b\"\n\n\ndef a(key: str):\n    res = b()\n    mapping = {\"c\": c}\n    sequence = [d]\n    return res, mapping[key](), sequence\n</code></pre> <p>After:</p> <pre><code>def a(key: str):\n    res = b()\n    mapping = {\"c\": c}\n    sequence = [d]\n    return res, mapping[key](), sequence\n\n\ndef b() -&gt; str:\n    return \"b\"\n\n\ndef c() -&gt; str:\n    return \"c\"\n\n\ndef d() -&gt; str:\n    return \"d\"\n</code></pre>"},{"location":"cases/#2-recursive-helper-chain","title":"2. Recursive Helper Chain","text":"<p>A recursive function stays first, followed by the non-recursive helpers it reaches.</p> <p>Before:</p> <pre><code>def leaf() -&gt; str:\n    return \"leaf\"\n\n\ndef helper() -&gt; str:\n    return leaf()\n\n\ndef recursive(items: list[str]) -&gt; str:\n    if items:\n        return recursive(items[1:])\n    return helper()\n</code></pre> <p>After:</p> <pre><code>def recursive(items: list[str]) -&gt; str:\n    if items:\n        return recursive(items[1:])\n    return helper()\n\n\ndef helper() -&gt; str:\n    return leaf()\n\n\ndef leaf() -&gt; str:\n    return \"leaf\"\n</code></pre>"},{"location":"cases/#3-nested-functions","title":"3. Nested Functions","text":"<p>Nested scopes are reordered independently, preserving the same top-down reading flow inside the outer function.</p> <p>Before:</p> <pre><code>def a() -&gt; str:\n    def b() -&gt; str:\n        def d() -&gt; str:\n            return \"d\"\n\n        def e() -&gt; str:\n            return d()\n\n        return e()\n\n    def c() -&gt; str:\n        return b()\n\n    return c()\n</code></pre> <p>After:</p> <pre><code>def a() -&gt; str:\n    def c() -&gt; str:\n        return b()\n\n    def b() -&gt; str:\n        def e() -&gt; str:\n            return d()\n\n        def d() -&gt; str:\n            return \"d\"\n\n        return e()\n\n    return c()\n</code></pre>"},{"location":"cases/#4-class-methods","title":"4. Class Methods","text":"<p>Methods are reordered within the class body. Instance and static method references follow the public method that uses them.</p> <p>Before:</p> <pre><code>class Greeting:\n    @staticmethod\n    def format_punctuation() -&gt; str:\n        return \"!\"\n\n    def format_name(self) -&gt; str:\n        return \"Ada\"\n\n    def greet(self) -&gt; str:\n        return f\"Hello, {self.format_name()}{Greeting.format_punctuation()}\"\n</code></pre> <p>After:</p> <pre><code>class Greeting:\n    def greet(self) -&gt; str:\n        return f\"Hello, {self.format_name()}{Greeting.format_punctuation()}\"\n\n    def format_name(self) -&gt; str:\n        return \"Ada\"\n\n    @staticmethod\n    def format_punctuation() -&gt; str:\n        return \"!\"\n</code></pre>"},{"location":"cases/#5-eager-definition-dependencies","title":"5. Eager Definition Dependencies","text":"<p>Some expressions are evaluated immediately: decorators, default arguments, and class-body values. Those dependencies stay above the chunk that needs them, even when a lazy caller can move first.</p> <p>Before:</p> <pre><code>def main() -&gt; str:\n    service = Service()\n    return service.handle()\n\n\ndef instrument(func):\n    return func\n\n\ndef normalize(value: str) -&gt; str:\n    return value.strip().upper()\n\n\nclass Service:\n    normalizer = normalize\n\n    @instrument\n    def handle(self, normalizer=normalize) -&gt; str:\n        return normalizer(\" ok \")\n</code></pre> <p>After:</p> <pre><code>def main() -&gt; str:\n    service = Service()\n    return service.handle()\n\n\ndef normalize(value: str) -&gt; str:\n    return value.strip().upper()\n\n\ndef instrument(func):\n    return func\n\n\nclass Service:\n    normalizer = normalize\n\n    @instrument\n    def handle(self, normalizer=normalize) -&gt; str:\n        return normalizer(\" ok \")\n</code></pre>"},{"location":"includes/cases/class-method-calls-instance-and-static-methods/","title":"Class method calls instance and static methods","text":"<p>Methods are reordered within the class body. Instance and static method references follow the public method that uses them.</p> <p>Before:</p> <pre><code>class Greeting:\n    @staticmethod\n    def format_punctuation() -&gt; str:\n        return \"!\"\n\n    def format_name(self) -&gt; str:\n        return \"Ada\"\n\n    def greet(self) -&gt; str:\n        return f\"Hello, {self.format_name()}{Greeting.format_punctuation()}\"\n</code></pre> <p>After:</p> <pre><code>class Greeting:\n    def greet(self) -&gt; str:\n        return f\"Hello, {self.format_name()}{Greeting.format_punctuation()}\"\n\n    def format_name(self) -&gt; str:\n        return \"Ada\"\n\n    @staticmethod\n    def format_punctuation() -&gt; str:\n        return \"!\"\n</code></pre>"},{"location":"includes/cases/eager-definition-dependencies/","title":"Eager definition dependencies","text":"<p>Some expressions are evaluated immediately: decorators, default arguments, and class-body values. Those dependencies stay above the chunk that needs them, even when a lazy caller can move first.</p> <p>Before:</p> <pre><code>def main() -&gt; str:\n    service = Service()\n    return service.handle()\n\n\ndef instrument(func):\n    return func\n\n\ndef normalize(value: str) -&gt; str:\n    return value.strip().upper()\n\n\nclass Service:\n    normalizer = normalize\n\n    @instrument\n    def handle(self, normalizer=normalize) -&gt; str:\n        return normalizer(\" ok \")\n</code></pre> <p>After:</p> <pre><code>def main() -&gt; str:\n    service = Service()\n    return service.handle()\n\n\ndef normalize(value: str) -&gt; str:\n    return value.strip().upper()\n\n\ndef instrument(func):\n    return func\n\n\nclass Service:\n    normalizer = normalize\n\n    @instrument\n    def handle(self, normalizer=normalize) -&gt; str:\n        return normalizer(\" ok \")\n</code></pre>"},{"location":"includes/cases/function-calls-recursive-function-and-helper/","title":"Function calls recursive function and helper","text":"<p>A recursive function stays first, followed by the non-recursive helpers it reaches.</p> <p>Before:</p> <pre><code>def leaf() -&gt; str:\n    return \"leaf\"\n\n\ndef helper() -&gt; str:\n    return leaf()\n\n\ndef recursive(items: list[str]) -&gt; str:\n    if items:\n        return recursive(items[1:])\n    return helper()\n</code></pre> <p>After:</p> <pre><code>def recursive(items: list[str]) -&gt; str:\n    if items:\n        return recursive(items[1:])\n    return helper()\n\n\ndef helper() -&gt; str:\n    return leaf()\n\n\ndef leaf() -&gt; str:\n    return \"leaf\"\n</code></pre>"},{"location":"includes/cases/function-contains-nested-function-references/","title":"Function contains nested function references","text":"<p>Nested scopes are reordered independently, preserving the same top-down reading flow inside the outer function.</p> <p>Before:</p> <pre><code>def a() -&gt; str:\n    def b() -&gt; str:\n        def d() -&gt; str:\n            return \"d\"\n\n        def e() -&gt; str:\n            return d()\n\n        return e()\n\n    def c() -&gt; str:\n        return b()\n\n    return c()\n</code></pre> <p>After:</p> <pre><code>def a() -&gt; str:\n    def c() -&gt; str:\n        return b()\n\n    def b() -&gt; str:\n        def e() -&gt; str:\n            return d()\n\n        def d() -&gt; str:\n            return \"d\"\n\n        return e()\n\n    return c()\n</code></pre>"},{"location":"includes/cases/function-references/","title":"Function references","text":"<p>The caller moves above the helpers it uses. Function bodies are lazy, so helpers may safely be defined below the caller.</p> <p>Before:</p> <pre><code>def d() -&gt; str:\n    return \"d\"\n\n\ndef c() -&gt; str:\n    return \"c\"\n\n\ndef b() -&gt; str:\n    return \"b\"\n\n\ndef a(key: str):\n    res = b()\n    mapping = {\"c\": c}\n    sequence = [d]\n    return res, mapping[key](), sequence\n</code></pre> <p>After:</p> <pre><code>def a(key: str):\n    res = b()\n    mapping = {\"c\": c}\n    sequence = [d]\n    return res, mapping[key](), sequence\n\n\ndef b() -&gt; str:\n    return \"b\"\n\n\ndef c() -&gt; str:\n    return \"c\"\n\n\ndef d() -&gt; str:\n    return \"d\"\n</code></pre>"}]}