### Function subject -> one class reference via annotation

Subject: one module-level function. References: one class. Reference form: parameter annotation. Expected order: subject first, then referenced class declaration.

Before:

```python
--8<-- "docs_src/cases/function_annotation_references_class/before.py"
```

After:

```python
--8<-- "docs_src/cases/function_annotation_references_class/after.py"
```
